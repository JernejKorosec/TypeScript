# TypeScript
About Typescript with simple examples.

# What is TypeScript
TypeScript is just a superset of javascript with types.
It's (strongly) typed javascript.

# Windows
```cmd
run.bat
```

### if you run
```cmd
tsc test.ts
```
### and get
```cmd
'tsc' is not recognized as an internal or external command, operable program or batch file.
```

### just run
```cmd
npm install -g typescript
```

### you also must add jsdoc as a node_module
```cmd
npm install -g jsdoc
```

### and than run  run.bat again
```cmd
run.bat
```

### and than just run
```cmd
jsdoc test1.js
```